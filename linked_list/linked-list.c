#include <stdio.h>
#include <stdlib.h>
#include "node.h"

typedef struct _node Node;


int main(int argc, char const *argv[]) {
    Node *head = NULL;
    int number;
    do {
        scanf("%d", &number);
        if (number != -1) {

        }
    } while (number != -1);
    return 0;
}
